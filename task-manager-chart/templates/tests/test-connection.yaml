apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "task-manager-chart.fullname" . }}-test-connection"
  labels:
    {{- include "task-manager-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "task-manager-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
